{{#breadcrumb.length}}
    <ol class="breadcrumb">
{{/breadcrumb.length}}
{{#breadcrumb}}
    {{#active}}
        <li class="active">{{text}}</li>
    {{/active}}
    {{^active}}
        <li><a href="{{url}}">{{text}}</a></li>
    {{/active}}
{{/breadcrumb}}
{{#breadcrumb.length}}
    </ol>
{{/breadcrumb.length}}